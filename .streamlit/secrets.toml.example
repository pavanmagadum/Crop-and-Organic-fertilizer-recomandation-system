# Streamlit Cloud Secrets Configuration
# Save this as secrets.toml in .streamlit folder when deploying to Streamlit Cloud

ADMIN_PASSWORD = "Admin@2025"

# For Streamlit Cloud deployment with persistent storage:
DATABASE_TYPE = "sqlite"
DB_PATH = "/app/.streamlit/community.db"

# OR if using PostgreSQL in the cloud:
# DATABASE_TYPE = "postgresql"
# DATABASE_URL = "postgresql://username:password@host:5432/database_name"
